version: 0.2
        
phases:
  install:
    on-failure: ABORT
    runtime-versions:
      nodejs: 20
  build:
    on-failure: ABORT
    commands:
      - cd front
      - yarn install
      - npm run build:deployment

artifacts:
  files:
    - front/dist/**/*